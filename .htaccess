# 10Projects.com — cPanel Routing
# Static HTML frontend + WordPress backend coexisting on same domain
#
# WordPress handles: wp-admin, wp-json (REST API), wp-login, sitemaps, cron
# Static HTML handles: all frontend pages (generated by Next.js export)

RewriteEngine On
RewriteBase /

# ------------------------------------------------------------------
# 1. Real files on disk → serve directly (CSS, JS, images, PHP, etc.)
# ------------------------------------------------------------------
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# ------------------------------------------------------------------
# 2. WordPress virtual paths → rewrite to index.php
#    (wp-json, wp-admin/ajax, sitemaps, robots.txt)
# ------------------------------------------------------------------
RewriteRule ^wp-json(/|$) /index.php [L]

RewriteCond %{REQUEST_URI} sitemap [NC]
RewriteRule ^ /index.php [L]

RewriteRule ^robots\.txt$ /index.php [L]
RewriteRule ^xmlrpc\.php$ /index.php [L]

# ------------------------------------------------------------------
# 3. Static HTML pages (Next.js export with trailingSlash)
# ------------------------------------------------------------------

# Directory with index.html → serve the static page
RewriteCond %{REQUEST_FILENAME}/index.html -f
RewriteRule ^(.*)/?$ $1/index.html [L]

# Clean URL → try appending /index.html
RewriteCond %{DOCUMENT_ROOT}/%{REQUEST_URI}/index.html -f
RewriteRule ^(.*)$ /$1/index.html [L]

# ------------------------------------------------------------------
# 4. Fallback → WordPress (catches everything else)
# ------------------------------------------------------------------
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]
